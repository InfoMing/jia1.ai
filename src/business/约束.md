# 前台目录约束

- 前台只保留首页和 `views/ai-prompts` 页面。
- 页面专用组件放入对应 `views` 目录，公共导航与页脚放入 `layout/components`。
- 静态资源必须放在 `business/assets`，不得依赖 `public` 旧站资源或线上运行时资源。
- AI 提示词为纯静态数据，不接 API、不使用 Mock、不写入本地存储。
- 站内跳转必须使用 Vue Router 命名路由，样式使用 `si-` 命名空间和 scoped SCSS。
